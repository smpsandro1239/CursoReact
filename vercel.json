{
  "version": 2,
  "buildCommand": "cd react-course && npm install && npm run build",
  "outputDirectory": "react-course/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
