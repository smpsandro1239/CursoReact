{
  "name": "premium-react-root",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd react-course && npm install && npm run build && cd .. && rm -rf dist && mv react-course/dist ./dist"
  },
  "author": "Sandro Pereira (smpsandro1239)"
}
